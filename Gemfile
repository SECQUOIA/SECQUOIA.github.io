source 'https://rubygems.org'

gem 'jekyll', '~> 4.3'

group :jekyll_plugins do
  gem 'jekyll-timeago', '~> 0.13.1'
end

# Explicitly require newer versions to avoid deprecation warnings
gem 'did_you_mean', '>= 1.6.0'
